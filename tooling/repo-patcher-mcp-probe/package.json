{
  "name": "mud-repo-patcher-mcp-probe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run typecheck && npm test",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "fixtures": "python scripts/generate_probe_inputs.py",
    "smoke": "node scripts/run_mcp_smoke.mjs",
    "smoke:matrix": "node scripts/run_mcp_smoke.mjs --matrix",
    "test": "vitest run --configLoader runner",
    "test:watch": "vitest --configLoader runner",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "0.20.1",
    "fflate": "0.8.3",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260809.1",
    "@modelcontextprotocol/client": "2.0.0",
    "typescript": "7.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.120.0"
  }
}
