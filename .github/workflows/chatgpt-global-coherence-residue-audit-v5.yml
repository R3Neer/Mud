name: ChatGPT global coherence residue audit v5
on:
  push:
    branches: [chatgpt-global-coherence-resume-runner-s]
permissions: {contents: read}
jobs:
  residue-audit:
    runs-on: windows-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v6
        with: {ref: chatgpt-global-coherence-resume-runner-s, path: runner, fetch-depth: 0}
      - uses: actions/checkout@v6
        with: {ref: chatgpt-global-coherence-resume-20260816-k, path: target, fetch-depth: 0}
      - name: Base candidata exacta
        shell: pwsh
        working-directory: target
        env: {BASE: 2d6390f19640a0e8b86889b5a3ea479bf7341dab}
        run: |
          if((git rev-parse HEAD).Trim() -ne $env:BASE){throw 'base mismatch'}
          if(git status --porcelain=v1 --untracked-files=all){throw 'dirty base'}
      - name: Barrido de residuos
        shell: pwsh
        working-directory: target
        run: |
          Copy-Item ../runner/tooling/chatgpt/resume_global_residue_audit.py ./audit.py
          python audit.py
          Remove-Item audit.py
